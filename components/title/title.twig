{% block title_prefix %}{% endblock %}
<{{ html_tag|default('h2') }}{{ attributes.addClass('umami-title').addClass(extra_classes|default([])) }}>
  {{ label }}
</{{ html_tag|default('h2') }}>
{% block title_suffix %}{% endblock %}
